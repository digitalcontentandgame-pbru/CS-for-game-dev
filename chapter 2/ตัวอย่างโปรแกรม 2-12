string s = "";
for (int i = 0; i < 100; i++) {
    s += i.ToString() + ", ";
}

var sb = new System.Text.StringBuilder();
for (int i = 0; i < 100; i++) {
    sb.Append(i);
    sb.Append(", ");
}
string result = sb.ToString();
