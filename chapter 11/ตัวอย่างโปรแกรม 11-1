public class Item
{
    public string Name { get; private set; }
    public int Power { get; private set; }
    public float Weight { get; private set; }

    public Item(string name, int power, float weight)
    {
        Name = name;
        Power = power;
        Weight = weight;
    }
}
